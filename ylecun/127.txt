The kernel function can be as complicated as you want. But if you learn it, then you are doing deep learning... This is what we do when we train Siamese networks (aka metric learning). It's like training a similarity kernel. You can stick a linear layer on top, if you want.